---
import AOS from "aos";
import "aos/dist/aos.css";
---

<script define:vars={{ AOS }}>
  document.addEventListener("astro:page-load", () => {
    AOS.init({
      duration: 2000,
      easing: "ease-out-cubic",
      once: true,
      offset: 100,
    });
  });
</script>